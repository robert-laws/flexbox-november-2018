mixin block(text)
  .block&attributes(attributes)
    p.block-text= text

mixin basic-block(text)
  .basic-block&attributes(attributes)
    p= text

mixin polygon(text)
  .box&attributes(attributes)
    h3= text